# Frontend

Frontend application for my-docs-ai-app.

## Getting Started

Open `index.html` in your browser or serve it with a local server:

```bash
python -m http.server 8000
# or
npx http-server
```

Visit `http://localhost:8000` to view the app.
